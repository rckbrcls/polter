# Archived Polter Code Notes

This directory contains retained legacy code. The active product is the Electron desktop app in `apps/desktop`, with shared non-visual logic in `packages/core`.

Do not treat this package as the current architecture, CLI workflow, packaging workflow, or default implementation reference. Only inspect it when a task explicitly asks for legacy behavior.

Keep any edits in this directory narrow, documented, and separate from active Electron or core changes whenever possible.
