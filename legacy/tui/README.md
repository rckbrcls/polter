# Archived Polter Code

> Status: retained legacy code.

This directory is kept in the repository for historical reference only. It is not the active Polter product, not the supported CLI surface, and not the source of truth for current architecture or development workflow.

Active work belongs in:

- `apps/desktop` for the Electron desktop app.
- `packages/core` for shared non-visual TypeScript services.
- `docs` for current project documentation.

Do not use this directory as a template for new runtime, UI, packaging, or release work unless a task explicitly asks to inspect legacy behavior.

The license file in this directory applies to this retained legacy package only. No root license file or root `license` field was identified for the active workspace.
